<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card text-center">
                <div class="card-header">
                    <h3>Nuevo Rol</h3>
                </div>
                <div class="card-body">
                <form action="/{{p.id}}" method="POST" enctype="multipart/form-data">
                        <div class="col-sm-6">  
                            <dl>
                                <dt><label for="resources">Nombre</label></dt>
                                <dd><input type="text" value="{{p.name}}" class="form-control" name="resources" placeholder="Nombre"  autofocus></dd>
                        </div>
                        <div class="form-group">
                            <button class="btn btn-success btn-block">
                                Añadir
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>