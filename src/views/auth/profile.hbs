<div class="container p-4">
    <div class="row">
        <div class="mx-auto">
            <div class="card text-center">
                <div class="card-body">
                    <h3>Bienvenido {{user.fullname}}</h3>
                    <h4>{{user.username}}</h4>
                    <a href="/logout" class="btn btn-primary m-4">
                        Cerrar Sesión
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>