<div class="container p-4">
    <div class="row">
        <div class="w-100 mx-auto">
            <div class="card">
                <div class="card-body">
                    <h2>Proyectos</h2>

                    <div class="form-group">
                        <button onclick="location.href='/projects/new'" class="btn btn-success">
                            Nuevo Proyecto
                        </button>
                    </div>
                    <table class="table">
                        <thead>
                            <th>ID</th>
                            <th>Nombre</th>
                            <th>Creado el</th>
                            <th>Editar</th>
                        </thead>
                        <tbody>
                        {{#each projects}}
                            <tr>
                                <td>{{id}}</td>
                                <td>{{name}}</td>
                                <td>{{timeago created_at}}</td>
                                <td>
                                    <button onclick="location.href = '/projects/edit/'+{{id}}" class="btn btn-warning">
                                        <i class="fas fa-edit"></i>
                                    </button>
                                </td>
                            </tr>
                        {{else}}
                            
                        {{/each}}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>